# Agent Skills

Do not add a custom skill yet.

Consider adding one later if we repeat any of these workflows beyond the existing docs:

- `deployment-runbook`: update, restart, inspect logs, roll back. Current generic notes live in `docs/deployment.md`.
- `source-evaluation`: evaluate new feeds for freshness, signal quality, and rate-limit risk.
- `notification-formatting`: tune Telegram message templates and escaping rules.